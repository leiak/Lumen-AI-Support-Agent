"""M1 agent layer — minimal SimpleResponder.

Stage 7+ will add a LangGraph-based responder alongside this one. The
``SimpleResponder`` shipped here is intentionally tiny so a Stage 7
implementer can ship a drop-in replacement behind the same call site.
"""